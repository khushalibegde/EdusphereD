import React from "react";
import { View, Text } from "react-native";

const MRPScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>MRP Screen</Text>
    </View>
  );
};

export default MRPScreen;
